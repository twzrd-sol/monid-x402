# Optional. discover/inspect on api.monid.ai still need a key.
# The x402 run host 402s without one.
# MONID_API_KEY=monid_live_...

# Pay path is not the default. Refuse never needs a wallet.
# PRIVATE_KEY=
