# VERIFY

Same-session grade of Default Path 1. Solo work does not get to call this
an independent lane. No spend. No payment header. 8787 left alone.

Re-run:

```bash
npm run build && node evidence/verify/grade.mjs
```

Machine record: `week2.json`.
